# Lane: Behavior and interoperability

Use for changed behavior, callers, UI, content, or save contracts. Does the task's
specified behavior work through the actual shared operations and state owners?

Show a concrete missing branch, ignored input, no-op, unconditional success,
unconnected required caller, or incompatible producer/consumer contract. Name
the file/line, trigger, caller, and wrong resulting state. Follow semantic input
through C# state changes to its published facts when that path changes.

A passing example alone does not establish completeness. A prerequisite can
legitimately precede its consumer when the task says so. Do not invent future
callers, additional game features, universal browser gates, or transaction
machinery that the task does not require.
